<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/patpat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/patpat"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApplication">
        <activity
            android:name=".FaqSuccessPopup"
            android:exported="true"
            android:theme="@style/CustomTheme"/>
        <activity
            android:name=".QuickLeaseSuccessPopup"
            android:exported="true"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".CustomerInquirySuccessPopup"
            android:exported="true"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TestApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SuccessPopup"
            android:theme="@style/CustomTheme" />
    </application>

</manifest>